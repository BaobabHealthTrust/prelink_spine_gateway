<script type="text/javascript" language="javascript">
  <!--
  tt_cancel_show = "/show/<%= @patient_id %>";
  tt_cancel_destination = "/show/<%= @patient_id %>";
  //-->
</script>

<%= form_tag(:action => "check_result_by_date") do %>

  <%= hidden_field_tag "patient_id", "#{@patient_id}" %>

  <%= text_field_tag "StartDate", nil,
    {
    :helptext => "Search Start Date",
    :field_type => 'date'
  }
%>

  <%= text_field_tag "EndDate", nil,
    {
    :helptext => "Search End Date",
    :field_type => 'date'
  }
%>

<% end %>